https://github.com/nltk/nltk_contrib

- `*.expected.ron` are transcribed by LLMs.
- `*.pympi-bin` are generated with `just gen-textgrid-pympi-bin`.

`demo_data3` is excluded because it doesn't seem to be a typical TextGrid file.
